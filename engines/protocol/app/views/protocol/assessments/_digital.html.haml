%h6 Digitalizações
%table.table.table-bordered.table-striped.table-celled
  %thead
    %tr
      %th #
      %th Página
      %th Documento
  %tbody.index-docs
    - if @digital_docs.present?
      - @digital_docs.each_with_index do |j, i|
        %tr
          %td.col-md-1
            = i + 1
          %td
            = j.page_number
          %td
            = link_to "Visualizar", j.doc_path_url, target: '_blank'

    - else
      %tr
        %td{colspan: 3}
          %i Nenhum registro encontrado.
