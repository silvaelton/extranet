.card
  .card-heading.custom-border-left
    .card-title
      Tipos de atendimento
      = "(#{@daily_types.count})"    
  .card-block
    %table.table.table-bordered.table-striped
      %thead
        %tr
          %th Nº
          %th Nome
          %th Descrição
          %th Status
          %th
      %tbody
        - if @daily_types.present?
          -@daily_types.order(:name).each do |daily_type|
            %tr
              %td.collapsing= daily_type.id
              %td= daily_type.name
              %td= daily_type.description
              %td.collapsing= daily_type.status? ? "Ativo" : "Inativo"
              %td.collapsing
                .dropdown
                  %button.btn.btn-success.btn-icon.btn-sm.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :type => "button"}
                    %i.fa.fa-cog
                    %i.ti-angle-down
                  .dropdown-menu.dropdown-menu-right{"aria-labelledby" => "dropdownMenuButton"}
                    = link_to "Excluir", daily_type_path(daily_type), method: :delete, class: 'dropdown-item', data: {confirm: t(:confirm)}, remote: true
                    = link_to "Editar", edit_daily_type_path(daily_type), class: 'dropdown-item', remote: true
        - else
          %tr
            %td{colspan: 6}
              %i Nenhum tipo de atendimento registrado.