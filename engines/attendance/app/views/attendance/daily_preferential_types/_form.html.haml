= simple_form_for @daily_preferential_type, remote: true do |f|
  = f.input :name, placeholder: 'Digite o tipo de boleto'
  = f.input :description, placeholder: 'Digite uma descrição'
  = f.input :status, as: :select, include_blank: 'selecione...'
  = f.button :submit, class: 'btn btn-success'