#sidebar
  .card
    .card-body.card-header.border-left-yellow
      .card-title 
        Filtro
        = link_to "javascript:void(0)", class: "collapsing-link" do
          %i.fa.fa-minus
    .card-body
      = simple_form_for url_for, method: :get, remote: true do |f|
        = f.input :by_name, as: :string, label: "Por nome", input_html: {name: :by_name, value: params[:by_name]}
        = f.input :by_publish, as: :select, label: "Publicado?", input_html: {name: :by_publish, value: params[:by_publish]}, include_blank: 'Selecione...'
        .mt-4
        = f.submit "Filtrar", name: nil, class: 'btn btn-primary btn-block'
        = link_to "Limpar", url_for, class: 'btn btn-dark btn-block'
  .divider
  .card
    .card-body.card-header.border-left-yellow
      .card-title 
        Operações
        = link_to "javascript:void(0)", class: "collapsing-link" do
          %i.fa.fa-minus
    .card-body.card-list
      = link_to "Adicionar menu",cms.new_nav_path, remote: true
      %hr/
      = link_to "Gestão de tipos",cms.nav_category_types_path
      = link_to "Gestão de categoria",cms.nav_categories_path