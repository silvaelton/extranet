= simple_form_for @registry_type, remote: true do |f|
  = f.input :name, placeholder: 'Digite o tipo de atividade'
  = f.input :status, as: :select, include_blank: 'selecione...'
  = f.button :submit, class: 'btn btn-success'
