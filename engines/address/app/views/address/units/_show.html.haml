.card
  .card-body.card-header.border-left-yellow
    .card-title
      Visão geral
  .header-content-new
    .card-body
      .row
        .col-8
          .row
            .col-12
              %p.title-sm= @unit.city.name rescue t(:no_info)
              %p.title
                = @unit.complete_address
              %p.title-sm= @unit.current_situation rescue t(:no_info)
        .col-4
          %table.table.table-striped.table-bordered
            %thead
              %tr
                %th Nome
                %th CPF
            %tbody
              %tr
                %td RII
                %td 126874596.5454

#unit-content
  .row.content
    .col-12
      .activity-content
        = render 'address/units/partials/activity'

  .row.content
    .col-12
      .image-content
        = render 'address/units/partials/image'

  .row.content
    .col-12
      .label-content
        = render 'address/units/partials/label'

  .row.content
    .col-12
      .registry-content
        = render 'address/units/partials/registry'
            
