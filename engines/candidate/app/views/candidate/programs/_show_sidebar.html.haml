#sidebar
  .card
    .card-body.card-header.border-left-yellow
      .card-title 
        Operações
        = link_to "javascript:void(0)", class: "collapsing-link" do
          %i.fa.fa-minus
    .card-body.card-list
      = link_to "Voltar para listagem", candidate.programs_path
      %hr/
      = link_to "Adicionar usuário (permissão)", candidate.new_program_program_user_path(@program), remote: true