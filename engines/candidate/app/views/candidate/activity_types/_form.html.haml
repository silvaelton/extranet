= simple_form_for @activity_type, remote: true do |f|

    .row
      .col-12 
        = f.input :name, as: :string
  
    .row
      .col-12 
        = f.input :status, as: :string
  
    .row
      .col-12 
        = f.input :description, as: :string
  
  .row
    .col-12
      = f.button :submit, class: 'btn btn-success', data: { disable_with: t(:disable_with) }