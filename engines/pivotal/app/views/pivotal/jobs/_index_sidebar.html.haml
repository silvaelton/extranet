#sidebar
  .card
    .card-body.card-header.border-left-yellow
      .card-title 
        Operações
        = link_to "javascript:void(0)", class: "collapsing-link" do
          %i.fa.fa-minus
    .card-body.card-list
      = link_to "Adicionar cargo", pivotal.new_job_path, remote: true, data: { disable_with: t(:disable_with)}
      %hr.divider
      = link_to "Gestão de usuários", pivotal.users_path, data: { disable_with: t(:disable_with)}
      = link_to "Gestão de setores", pivotal.sectors_path, data: { disable_with: t(:disable_with)}
      = link_to "Gestão de engines", pivotal.engines_path, data: { disable_with: t(:disable_with)}
