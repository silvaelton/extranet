= simple_form_for [@topic, @page_user], remote: true do |f|

  .row
    .col-12 
      = f.input :staff_id, collection: Pivotal::User.status_active, include_blank: 'Selecione...'
  
  .row
    .col-12
      = f.button :submit, class: 'btn btn-success', data: { disable_with: t(:disable_with) }